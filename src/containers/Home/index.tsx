import Button from "components/Button";

const Home = () => {
  return (
    <div className="container mx-auto flex min-h-screen w-screen">
      <Button label="test" fill={false}/>
    </div>
  );
};

export default Home;
